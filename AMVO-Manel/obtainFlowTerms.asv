function [Fe, Fw, Fn, Fs] = obtainFlowTerms (U,V,h,i,j)

    Fs = h * avgField( V(i-1,j-1), V(i,j-1) );
    Fn = h * avgField( V(i-1,j)  , V(i+1,j)   );
    Fe = h * avgField( U(i+1,j), U(i,j)     );
    Fw = h * avgField( U(i-1,j), U(i,j)     );

end